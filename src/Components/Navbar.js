import React from "react";

const Navbar = () => {
  return (
    <React.StrictMode>
      <nav>
        <h4 className="navTitle" id="navTitle">
          <a href="/">NewsBite</a>
        </h4>
      </nav>
    </React.StrictMode>
  );
};

export default Navbar;
